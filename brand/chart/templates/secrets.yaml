apiVersion: v1
kind: Secret
metadata:
  name: eru-brand-secrets
  labels:
    app: {{ include "eru-brand.name" . }}
type: Opaque
data:
  {{- with .Values.secrets }}
  {{- range $key, $value := . }}
  {{ $key | quote }}: {{ $value | b64enc | quote }}
  {{- end }}
  {{- end }}